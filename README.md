# REDLINE

A mobile-first browser reflex game.

## Run locally
Open `index.html` in a modern browser.

## Publish
Upload the project to a static host such as GitHub Pages or itch.io.

## Assets
The first version uses only original HTML/CSS/JavaScript and no external assets.
